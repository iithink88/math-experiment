---
name: math-experiment
description: 2D/3D 数学实验方案设计与交互式动画可视化工具；当用户需要进行2D函数绘图、3D立体图形展示、探索数学规律或可视化几何关系时使用
---

# 数学实验技能

本技能用于创建交互式数学实验方案，生成独立运行的 HTML 文件进行 2D/3D 图形可视化和动画展示。

## 任务目标
- 设计完整的数学实验方案（包含实验目的、参数设计、预期观察点）
- 通过参数调整探索 2D/3D 数学规律
- 生成独立运行的 HTML 文件，支持 2D 和 3D 交互式动画
- 支持变换路径追踪和主题切换功能

## 操作步骤

### 阶段1：生成数学实验方案
1. 理解用户需求，明确实验类型：

   **2D 实验类型**:
   - 函数性质探索（如二次函数、三角函数、指数函数）
   - 统计数据分析（如分布拟合、回归分析、直方图）
   - 平面几何验证（如圆、椭圆、三角形性质）
   - 微积分可视化（如极限、导数、积分）

   **3D 实验类型**:
   - 3D 函数曲面（如 z = f(x, y)）
   - 空间几何体（如球体、立方体、圆柱、圆锥）
   - 3D 曲线（如螺旋线、空间参数曲线）
   - 旋转体和曲面
   - 3D 向量和空间关系

2. 生成实验方案，包含以下内容：
   - 实验目的：明确要探索的数学问题
   - 维度选择：2D 或 3D，根据问题性质决定
   - 核心参数：列出可调整的关键参数（如 a, b, c, k 等）
   - 参数范围：为每个参数设定合理的初始值和调整范围
   - 观察要点：描述用户应关注的图形变化规律
   - 实验步骤：建议的参数调整顺序和观察方法

3. 向用户展示实验方案，等待确认或调整
   - **重要**：必须等待用户明确确认实验方案后，才能进入阶段3生成 HTML 文件
   - 用户确认方式：用户明确表示"确认"、"可以"、"好的"等肯定答复，或主动要求生成实验

### 阶段2：参数调整与观察
1. 根据用户反馈优化参数设计
2. 提供参数调整建议，解释不同参数值对图形的影响
3. 引导用户通过参数变化发现数学规律
4. 说明动画效果的使用（如有）
5. **重要**：用户修改参数需求后，需重新展示调整后的实验方案，并再次等待用户确认

### 阶段3：生成可视化 HTML 文件
1. 根据维度选择对应模板：
   - **2D 模板**: [assets/math-experiment-template.html](assets/math-experiment-template.html)（基于 Plotly.js）
   - **3D 模板**: [assets/math-experiment-3d-template.html](assets/math-experiment-3d-template.html)（基于 Three.js）

2. 根据实验方案定制以下内容：
   - **2D 模板**:
     - 数学表达式：将用户的问题转化为 Plotly.js 可识别的表达式
     - 参数控制：为每个核心参数创建滑块或输入框
     - 图形配置：设置坐标轴范围、标签、标题
     - 交互逻辑：实现参数变化时图形的实时更新
   - **3D 模板**:
     - 3D 表达式：定义 z = f(x, y) 或几何体参数
     - 参数控制：创建滑块控制 3D 图形的变换
     - 场景配置：设置相机位置、光源、背景
     - 动画逻辑：实现旋转、缩放、参数动画

3. 生成完整的 HTML 文件，确保：
   - 所有依赖通过 CDN 引入（Plotly.js 或 Three.js）
   - 可在浏览器中直接打开
   - 无需服务器环境
   - 支持主流浏览器
   - 支持 2D/3D 交互和动画

4. 提供联想优化建议
   - 在交付 HTML 文件后，主动提示用户可以考虑的优化方向
   - 根据当前实验类型，提供针对性的功能完善建议

## 资源索引
- **2D HTML 模板**: 见 [assets/math-experiment-template.html](assets/math-experiment-template.html)
  - 基于 Plotly.js 库
  - 支持函数绘图、统计图表
  - 支持变换路径显示（历史图形追踪）
  - 支持暗色/亮色主题切换
  - 适用于平面函数和统计分析

- **3D HTML 模板**: 见 [assets/math-experiment-3d-template.html](assets/math-experiment-3d-template.html)
  - 基于 Three.js 库
  - 支持 3D 函数曲面、空间几何体
  - 支持实时动画和交互操作（旋转、缩放、平移）
  - 支持变换路径显示（历史 3D 网格追踪）
  - 支持暗色/亮色主题切换
  - 适用于 3D 曲面和立体图形

## 注意事项
- **确认流程强制要求**：生成实验设计方案后，必须等待用户明确确认，未经确认不得直接生成 HTML 文件
- **参数设计原则**：参数数量控制在 3-5 个，每个参数的作用清晰明确
- **维度选择**：根据问题性质选择 2D 或 3D，2D 适用于平面函数，3D 适用于空间曲面和几何体
- **数学表达式转换**：
  - 2D：使用 JavaScript 数学函数（如 Math.sin, Math.pow）
  - 3D：使用 Three.js 几何体 API 或自定义顶点计算
- **图形性能优化**：
  - 2D：避免在参数变化时重新计算过多数据点
  - 3D：控制网格密度，避免过多的顶点数
- **用户体验**：提供清晰的参数标签、单位和说明
- **兼容性**：使用标准 Web 技术，确保在主流浏览器中正常运行
- **变换路径功能**：默认显示参数调整后的图形变换路径，每条路径使用不同颜色区分，可通过按钮切换显示/隐藏
- **主题切换**：提供亮色/暗色主题切换功能，适配不同使用环境
- **动画效果**：
  - 2D：通过参数变化实现动态图形更新
  - 3D：支持自动旋转、参数动画、实时渲染

## 联想优化建议

在实验动画图形设计完成后，根据实验类型提供以下优化建议：

### 通用优化建议
- **数据导出功能**：支持将实验数据导出为 CSV、JSON 或图片格式
- **参数预设方案**：提供常用参数组合的快速切换按钮
- **历史记录管理**：支持保存、加载和分享实验配置
- **多视图对比**：支持同时显示多个参数配置的图形进行对比
- **实时数据统计**：显示关键数值变化（极值、平均值、增长率等）

### 2D 实验专属优化
- **函数标注**：自动标注关键点（极值、零点、拐点、渐近线等）
- **动态公式推导**：实时显示数学推导过程和中间步骤
- **图形分析工具**：提供测量工具（距离、面积、斜率等）
- **图表类型切换**：支持在不同图表类型间切换（如散点图、线图、柱状图）
- **动画控制增强**：添加播放/暂停/步进功能，支持动画速度调节
- **数据拟合**：自动拟合曲线并显示拟合优度

### 3D 实验专属优化
- **截面分析**：支持在不同平面（XY、XZ、YZ）上显示截面图
- **等高线叠加**：在 3D 曲面上叠加等高线显示
- **光照控制**：提供更多光源选项（点光源、平行光、环境光）
- **材质切换**：支持不同材质效果（金属、玻璃、纹理等）
- **视角预设**：提供常用视角快速切换（顶视图、侧视图、等轴视图）
- **体积可视化**：支持体渲染技术显示内部结构
- **切片功能**：支持对 3D 模型进行切片查看

### 交互增强优化
- **鼠标提示**：鼠标悬停时显示详细数值和说明
- **手势控制**：支持触摸屏手势操作（缩放、旋转、平移）
- **键盘快捷键**：提供常用操作的快捷键支持
- **响应式布局**：自适应不同屏幕尺寸和分辨率
- **全屏模式**：支持全屏显示以获得更好的观察体验

### 教学辅助优化
- **步骤引导**：提供实验操作步骤的分步提示
- **问题嵌入**：在关键节点嵌入思考题或练习题
- **笔记功能**：支持在实验过程中添加笔记和标注
- **结果分享**：支持将实验结果和配置分享给其他用户
- **多语言支持**：支持中英文或其他语言切换

**使用原则**：
- 在交付 HTML 文件后，根据当前实验的特性选择 3-5 个最相关的优化建议
- 优先推荐能够提升教学效果或用户体验的功能
- 简要说明每个优化建议的价值和实现难度

## 常见应用场景

### 2D 函数探索
- 二次函数：y = ax² + bx + c，探索 a、b、c 对开口方向、顶点位置的影响
- 三角函数：y = A·sin(ωx + φ)，探索振幅、频率、相位的影响
- 指数函数：y = a·e^(bx)，探索增长率的影响

### 统计分析
- 正态分布：调整 μ（均值）和 σ（标准差），观察分布形态变化
- 回归分析：生成散点图和回归曲线，探索数据拟合效果
- 直方图：调整分组数量，观察数据分布特征

### 平面几何
- 圆与椭圆：调整参数方程，探索图形性质
- 三角形构造：通过坐标变化探索几何关系
- 参数方程：x = f(t), y = g(t)，可视化参数曲线

### 3D 可视化
- 3D 函数曲面：z = ax² + by² + c，探索抛物面的形状变化
- 空间曲面：z = sin(ax) + cos(by)，观察波浪曲面
- 几何体变换：球体、立方体、圆柱的缩放、旋转、平移
- 3D 曲线：螺旋线、空间参数曲线
- 旋转体：将 2D 曲线绕轴旋转生成立体图形
